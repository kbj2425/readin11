<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>1단계: 안구 회전 훈련 - READIN</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #FFC0CB, #FFB6C1);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            background: white;
            border-radius: 20px;
            padding: 50px;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            text-align: center;
        }

        .step-indicator {
            color: #FF69B4;
            font-size: 1.2em;
            font-weight: bold;
            margin-bottom: 10px;
        }

        h1 {
            color: #333;
            margin-bottom: 30px;
            font-size: 2em;
        }

        .instruction {
            background: #f8f9fa;
            padding: 30px;
            border-radius: 15px;
            margin: 30px 0;
            font-size: 1.3em;
            color: #666;
            line-height: 1.8;
        }

        .checkbox-container {
            margin: 40px 0;
        }

        .checkbox-label {
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 1.2em;
            color: #333;
        }

        .checkbox-label input[type="checkbox"] {
            width: 30px;
            height: 30px;
            margin-right: 15px;
            cursor: pointer;
        }

        .next-button {
            background: #FFC0CB;
            color: white;
            border: none;
            padding: 20px 50px;
            border-radius: 50px;
            font-size: 1.3em;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s;
            opacity: 0.5;
            pointer-events: none;
        }

        .next-button.active {
            opacity: 1;
            pointer-events: auto;
        }

        .next-button.active:hover {
            background: #FF69B4;
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(255, 105, 180, 0.4);
        }

        @media (max-width: 768px) {
            .container {
                padding: 30px 20px;
            }

            h1 {
                font-size: 1.5em;
            }

            .instruction {
                font-size: 1.1em;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="step-indicator">1/5 단계</div>
        <h1>안구 회전 훈련을 시작하세요</h1>
        
        <div class="instruction">
            👁️ 눈동자를 천천히 원을 그리듯 회전시켜주세요<br>
            ↔️ 좌우, 상하로 움직여주세요<br>
            ⏱️ 약 2-3분간 진행해주세요
        </div>

        <div class="checkbox-container">
            <label class="checkbox-label">
                <input type="checkbox" id="completeCheck" onchange="enableNext()">
                <span>안구 회전 훈련을 완료했습니다</span>
            </label>
        </div>

        <button class="next-button" id="nextButton" onclick="goNext()">
            다음 단계로 →
        </button>
    </div>

    <script>
        function enableNext() {
            const checkbox = document.getElementById('completeCheck');
            const button = document.getElementById('nextButton');
            
            if (checkbox.checked) {
                button.classList.add('active');
            } else {
                button.classList.remove('active');
            }
        }

        async function goNext() {
            const checkbox = document.getElementById('completeCheck');
            
            if (!checkbox.checked) {
                alert('체크박스를 선택해주세요.');
                return;
            }

            try {
                const response = await fetch('/complete-step', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ step: 1 })
                });

                const result = await response.json();
                
                if (result.success) {
                    window.location.href = '/step2-teacher';
                } else {
                    alert('저장에 실패했습니다.');
                }
            } catch (error) {
                console.error('Error:', error);
                alert('서버 오류가 발생했습니다.');
            }
        }
    </script>
</body>
</html>
