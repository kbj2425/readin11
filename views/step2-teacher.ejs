<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>2단계: 선생님 한마디 - READIN</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #FFC0CB, #FFB6C1);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            background: white;
            border-radius: 20px;
            padding: 50px;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            text-align: center;
        }

        .step-indicator {
            color: #FF69B4;
            font-size: 1.2em;
            font-weight: bold;
            margin-bottom: 10px;
        }

        h1 {
            color: #333;
            margin-bottom: 30px;
            font-size: 2em;
        }

        .instruction {
            background: #f8f9fa;
            padding: 30px;
            border-radius: 15px;
            margin: 30px 0;
            font-size: 1.3em;
            color: #666;
            line-height: 1.8;
        }

        .highlight-icon {
            font-size: 3em;
            margin: 20px 0;
        }

        .checkbox-container {
            margin: 40px 0;
        }

        .checkbox-label {
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 1.2em;
            color: #333;
        }

        .checkbox-label input[type="checkbox"] {
            width: 30px;
            height: 30px;
            margin-right: 15px;
            cursor: pointer;
        }

        .button-container {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-top: 40px;
        }

        .action-button {
            flex: 1;
            max-width: 250px;
            padding: 20px 40px;
            border: none;
            border-radius: 15px;
            font-size: 1.2em;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s;
        }

        .complete-button {
            background: linear-gradient(135deg, #FFC0CB, #FF69B4);
            color: white;
            box-shadow: 0 5px 15px rgba(255, 105, 180, 0.3);
        }

        .complete-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(255, 105, 180, 0.4);
        }

        .skip-button {
            background: #6c757d;
            color: white;
            box-shadow: 0 5px 15px rgba(108, 117, 125, 0.3);
        }

        .skip-button:hover {
            background: #545b62;
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(108, 117, 125, 0.4);
        }

        @media (max-width: 768px) {
            .container {
                padding: 30px 20px;
            }

            h1 {
                font-size: 1.5em;
            }

            .instruction {
                font-size: 1.1em;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="step-indicator">2/5 단계</div>
        <h1>'선생님 한마디'를<br>형광펜으로 밑줄 그으면서 읽으세요</h1>
        
        <div class="highlight-icon">🖍️</div>
        
        <div class="instruction">
            📖 독서노트의 '선생님 한마디' 부분을 찾으세요<br>
            ✏️ 형광펜을 준비하세요<br>
            📝 '선생님 한마디'를 밑줄을 그으며 읽으세요
        </div>

        <div class="button-container">
            <button class="action-button complete-button" onclick="goNext()">
                ✅ 완료했어요!
            </button>
            <button class="action-button skip-button" onclick="skipStep()">
                하지 않아요
            </button>
        </div>
    </div>

    <script>
        async function goNext() {
            try {
                const response = await fetch('/complete-step', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ step: 2 })
                });

                const result = await response.json();
                
                if (result.success) {
                    window.location.href = '/step3-notebook';
                } else {
                    alert('저장에 실패했습니다.');
                }
            } catch (error) {
                console.error('Error:', error);
                alert('서버 오류가 발생했습니다.');
            }
        }

        async function skipStep() {
            if (!confirm('선생님 한마디 읽기를 건너뛰시겠습니까?')) {
                return;
            }

            try {
                const response = await fetch('/complete-step', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ step: 2 })
                });

                const result = await response.json();
                
                if (result.success) {
                    window.location.href = '/step3-notebook';
                } else {
                    alert('저장에 실패했습니다.');
                }
            } catch (error) {
                console.error('Error:', error);
                alert('서버 오류가 발생했습니다.');
            }
        }
    </script>
</body>
</html>
